const tableau = [1, 'Hello', {a: "Test"}];
             //  int, String, Object

tableau.push(2);
        // [1, 'Hello', {a: "Test"}, 2]
        //  0     1       2          3

        tableau.forEach(element => {
            console.log(element);
        });

        { key: "Seven", new_key: "Academy" }